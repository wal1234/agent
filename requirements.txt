# LLM SDK
anthropic>=0.40.0
openai>=1.40.0

# 数据处理
pandas>=2.0.0
pyyaml>=6.0
python-dateutil>=2.8.2

# CLI
click>=8.1.0
rich>=13.0.0

# V2: API 服务化
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.7.0
python-multipart>=0.0.9    # FastAPI 表单/文件上传

# 注意：当前自实现轻量编排器（src/graph/analysis_graph.py），未引入 langgraph 包
# 升级 Python 至 3.10+ 后可启用 langgraph>=0.2.50

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.27.0              # FastAPI TestClient 依赖
